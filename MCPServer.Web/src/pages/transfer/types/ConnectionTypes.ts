/**
 * Re-exporting all connection types from the single source of truth
 * @deprecated Use '../types/Connection' directly instead
 */

import { 
  Connection, 
  ConnectionTestResult, 
  ConnectionAccessLevel
} from '../types/Connection';

export { 
  Connection, 
  ConnectionTestResult, 
  ConnectionAccessLevel
};